import React from "react";
import styles from "./Banner.module.css";

const Banner = () => {
  return (
    <div class={styles.bannerDiv}>
      <p class={styles.per}>PERMANENT CAMPUS</p>
    </div>
  );
};

export default Banner;
